#include <stdio.h>
#include <stdlib.h>
#include <string.h>
void given_char_occurance(char[], int, char);
void string_length(char[]);
void strlen_string(char[]);
void to_upper_string(char[]);
void to_lower_string(char[]);
void string_reverse_using_strrev(char[]);
void string_reverse(char[]);

int main()
{
    string_reverse("Hello");
    return 0;
}
